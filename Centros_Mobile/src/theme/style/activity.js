export default (theme) => ({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    with: 'auto',
    
  },
  title: {
    fontSize: 20,
    fontWeight: 'bold',
    color: theme.colors.text,
  },
});